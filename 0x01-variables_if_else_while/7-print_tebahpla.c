#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char alphar;

	for (alphar = 'z' ; alpha >= 'a' ; alphar++)
	{
		putchar(alpha);
	}
	putchar('\n');

	return (0);
}
