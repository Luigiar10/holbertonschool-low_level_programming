#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */

int main(void)
{
	char alpha;
	char q = 'q';
	char e = 'e';

	for (alpha = 'a' ; alpha <= 'z' ; alpha++)
	{
		if (alpha != q && alpha != e)
		{
			putchar(c);
		}
	}
	putchar('\n');

	return (0);
}
